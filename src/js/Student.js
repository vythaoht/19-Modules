//export default:
// - 1 module chi co the export default 1 lan
// - khi ta import co the dat ten bien tuy y va khong can dau {}
export default class Student {
  constructor(id, name, email) {
    this.id = id;
    this.name = name;
    this.email = email;
  }
}
